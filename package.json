{
  "name": "aegis-qr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build-all": "npm run build --prefix backend && npm run build --prefix frontend",
    "start-prod": "NODE_ENV=production npm start --prefix backend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend"
  }
}
